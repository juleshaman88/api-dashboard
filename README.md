Eight different APIs. 
Click on each box to discover more information about the selected niche.
Each API pulls from databases set to public access. Two of these required API access keys. 
At the bottom is a button to clear all of the boxes and start over.
